namespace: cert-manager
helm:
  repo: "https://charts.jetstack.io"
  chart: cert-manager
  version: "1.16.2"
  releaseName: cert-manager
  values:
    crds:
      enabled: true
labels:
  app: cert-manager
